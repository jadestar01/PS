#include <iostream>

using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    long long int  x, y, Z;
    cin >> x >> y >> Z;

cout << x + y + Z;

    return 0;
}